**8. The "index as Key" Bug**

const [todos,setTodos] = useState([
{id:1, text:"Walk dog"},
{id:2. text:"Buy milk"}
]);
const removeTodo =(id) => {
setTodos(todos.filter(todo => todo.id !== id));
};
{
todos.map(todo => {
<div key ={todo.id}>
  <input defaultValue = {todo.text}/>
  <button onClick = {() => 
    removeTodo(todo.id)}>Delete</button>
</div>
))}
